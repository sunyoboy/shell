#数组name的下标是用户自定义的变量x。运算符++表明这是一个数值型的变量。awk将x初始化为0，并且每次使用x后将其加1(所用的是后递增运算符)。每条记录的第2个字段都将赋值给数组name中的相应元素。END块使用for循环来循环处理数组，将从下标0开始，依次打印数组元素的值。下标只是一个键，所以不必从0开始。下标可以从任意值开始，数字或字符串都可以。
{id[NR]=$3}END{for(x=1;x<=NR;x++){print id[x], NR }}